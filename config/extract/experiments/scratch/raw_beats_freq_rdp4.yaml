# @package _global_
restore_or_scratch: scratch

infer_ver: null

scratch_frontend:
  tgt_class: asdkit.frontends.pretrained_feature.beats.BEATsFrequencyPoolingFrozenModel
  pooling: rdp
  gamma: 4
  emit_patch_sequence: false
  model_cfg:
    ckpt_path: "pretrained_models/beats/BEATs_iter3.pt"

datamodule:
  train:
    collator:
      sec: ${dcase}
